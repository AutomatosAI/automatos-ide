node_modules/
dist/
coverage/
*.vsix
.vscode-test/
.DS_Store
*.log

# Throwaway control repo for F5 live-testing (see scripts/seed-dev-workspace.mjs)
.dev-workspace/
.dev-workspace-remote.git/

# Per-worker git worktrees created when launching a CLI worker on a card
.automatos-worktrees/

# Local worker heartbeats — liveness is "is the engine terminal still open" on THIS
# machine, so beats stay local and are never committed (no cross-machine git thrash).
.heartbeats/
